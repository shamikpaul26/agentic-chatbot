# Agentic Chatbot

This project implements a conversational AI chatbot using LangGraph for stateful conversation management and Streamlit for the web-based user interface. The chatbot maintains message history across turns using LangGraph's built-in memory checkpointing and leverages OpenAI's GPT-3.5-Turbo model for natural language responses.

